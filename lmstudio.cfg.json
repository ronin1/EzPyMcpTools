{
  "mcpServers": {
    "ezpy_tools_docker": {
      "command": "docker",
      "args": [
        "run",
        "--rm",
        "-i",
        "-v",
        "$PWD/user.data.json:/app/user.data.json:ro",
        "ezpy-tools:alpine"
      ]
    }
  }
}