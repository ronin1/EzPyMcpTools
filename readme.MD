# Install Instruction

Use this to install as LMStudio MCP Server
```json
{
  "mcpServers": {
    "tools": {
      "command": "uv",
      "args": ["run", "python", "mcp_server.py"],
      "cwd": "~/ai/tools"
    }
  }
}
```
